# CortexWard workspace root.
#
# This file is a uv *workspace* manifest, not an installable distribution
# (see [tool.uv] package = false below). Each real package lives under
# packages/<name>/ with its own [project]/[build-system]. Shared dev tooling
# (lint/type/test) and its configuration live here so every package in the
# workspace is held to one standard (see ADR-0005).
[project]
name = "cortexward-workspace"
version = "0.0.0"
description = "CortexWard workspace root (not published; see packages/ for distributions)."
requires-python = ">=3.11"

[project.optional-dependencies]
dev = [
  "ruff>=0.16.1",
  "mypy>=2.3.0",
  "pytest>=8.2",
  "pytest-cov>=5.0",
  "hypothesis>=6.165.0",
  "import-linter>=2.1",
  "types-PyYAML>=6.0",
  "httpx>=0.27,<1",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/*"]

# ---------------------------------------------------------------------------
# Ruff — linter + formatter, shared across every package in the workspace.
# ---------------------------------------------------------------------------
[tool.ruff]
line-length = 100
target-version = "py311"
# Extend this list as new packages/<name>/{src,tests} directories are added.
src = [
  "packages/cortexward-core/src", "packages/cortexward-core/tests",
  "packages/cortexward-cpg/src", "packages/cortexward-cpg/tests",
  "packages/cortexward-scanners/src", "packages/cortexward-scanners/tests",
  "packages/cortexward-reporters/src", "packages/cortexward-reporters/tests",
  "packages/cortexward-eval/src", "packages/cortexward-eval/tests",
  "packages/cortexward-llm/src", "packages/cortexward-llm/tests",
  "packages/cortexward-orchestrator/src", "packages/cortexward-orchestrator/tests",
  "packages/cortexward-cli/src", "packages/cortexward-cli/tests",
  "packages/cortexward-agents/src", "packages/cortexward-agents/tests",
  "packages/cortexward-server/src", "packages/cortexward-server/tests",
  "packages/cortexward-vcs/src", "packages/cortexward-vcs/tests",
  "packages/cortexward-storage/src", "packages/cortexward-storage/tests",
  "packages/cortexward-sandbox/src", "packages/cortexward-sandbox/tests",
]
# The golden benchmark dataset's examples are deliberately vulnerable (and
# deliberately clean) *data*, not application source — linting them as if
# they needed this project's own house style/security rules would be
# self-defeating (the vulnerable ones exist specifically to trip a real
# scanner; ruff flagging the same issue isn't a bug in the dataset).
extend-exclude = ["packages/cortexward-eval/datasets/**"]

[tool.ruff.lint]
select = [
  "E", "W",   # pycodestyle
  "F",        # pyflakes
  "I",        # isort
  "N",        # pep8-naming
  "UP",       # pyupgrade
  "B",        # flake8-bugbear
  "C4",       # comprehensions
  "SIM",      # flake8-simplify
  "TID",      # tidy imports
  "RUF",      # ruff-specific
  "S",        # flake8-bandit (security)
  "PL",       # pylint
]
ignore = [
  "PLR0913",  # many arguments — common for rich domain constructors
]

[tool.ruff.lint.per-file-ignores]
"**/tests/**/*.py" = ["S101", "PLR2004", "S106"]  # asserts, magic values, and fake credential
# fixture args (e.g. token="t") are fine in tests

[tool.ruff.lint.isort]
known-first-party = ["cortexward", "tests"]

# ---------------------------------------------------------------------------
# mypy — strict typing across the workspace.
# ---------------------------------------------------------------------------
# mypy is invoked once per package (see the `mypy` CI step / CONTRIBUTING.md),
# not with every package's tests/ in one run: each package's tests/ has its
# own conftest.py, and with no __init__.py under tests/ (required so sibling
# packages' tests/ trees don't collide as the top-level module "tests" — see
# the pytest importlib note above), mypy would otherwise see two same-named
# top-level "conftest" modules and refuse to proceed. mypy_path still spans
# every package's src/ so cross-package imports (e.g. cortexward-cpg
# importing cortexward.domain/cortexward.ports from cortexward-core) resolve
# regardless of which package is being checked.
[tool.mypy]
python_version = "3.11"
strict = true
warn_unreachable = true
warn_redundant_casts = true
disallow_untyped_defs = true
plugins = ["pydantic.mypy"]
explicit_package_bases = true
# Golden-dataset fixtures are deliberately-vulnerable/untyped benchmark DATA
# (ward bench ground truth), not application source code — same reasoning as
# the ruff extend-exclude for this path.
exclude = ["^packages/cortexward-eval/datasets/"]
mypy_path = [
  "packages/cortexward-core/src",
  "packages/cortexward-cpg/src",
  "packages/cortexward-scanners/src",
  "packages/cortexward-reporters/src",
  "packages/cortexward-eval/src",
  "packages/cortexward-llm/src",
  "packages/cortexward-orchestrator/src",
  "packages/cortexward-cli/src",
  "packages/cortexward-agents/src",
  "packages/cortexward-server/src",
  "packages/cortexward-vcs/src",
  "packages/cortexward-storage/src",
  "packages/cortexward-sandbox/src",
]

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false

[tool.pydantic-mypy]
init_forbid_extra = true
init_typed = true
warn_required_dynamic_aliases = true

# ---------------------------------------------------------------------------
# pytest.
# ---------------------------------------------------------------------------
[tool.pytest.ini_options]
minversion = "8.0"
# importlib import mode identifies test modules by file path rather than by a
# dotted package name inserted into sys.path. Every workspace package has its
# own tests/ directory; without this, two packages' tests/ trees would both
# claim the top-level module name "tests" and collide in sys.modules.
addopts = "-ra --strict-markers --strict-config --import-mode=importlib"
testpaths = [
  "packages/cortexward-core/tests",
  "packages/cortexward-cpg/tests",
  "packages/cortexward-scanners/tests",
  "packages/cortexward-reporters/tests",
  "packages/cortexward-eval/tests",
  "packages/cortexward-llm/tests",
  "packages/cortexward-orchestrator/tests",
  "packages/cortexward-cli/tests",
  "packages/cortexward-agents/tests",
  "packages/cortexward-server/tests",
  "packages/cortexward-vcs/tests",
  "packages/cortexward-storage/tests",
  "packages/cortexward-sandbox/tests",
]
markers = [
  "unit: fast, isolated unit tests",
  "integration: cross-component integration tests",
  "security: adversarial / security-focused tests",
]

[tool.coverage.run]
branch = true
source = ["cortexward"]

[tool.coverage.report]
show_missing = true
skip_covered = true

# ---------------------------------------------------------------------------
# import-linter — enforces the hexagonal dependency direction (ADR-0005).
# ---------------------------------------------------------------------------
[tool.importlinter]
root_packages = ["cortexward"]

# The overarching dependency direction (MPS §8.2: "dependencies point inward").
# As adapters/application/interface packages land in later phases, add their
# top-level modules above "cortexward.plugins" here, highest (most dependent)
# first.
[[tool.importlinter.contracts]]
name = "Layered dependency direction (core)"
type = "layers"
layers = [
  "cortexward.plugins",
  "cortexward.ports",
  "cortexward.domain",
]

[[tool.importlinter.contracts]]
name = "Domain core stays pure"
type = "forbidden"
source_modules = ["cortexward.domain"]
forbidden_modules = [
  "cortexward.ports",
  "cortexward.plugins",
  "cortexward.cpg",
  "cortexward.scanners",
  "cortexward.reporters",
  "cortexward.eval",
  "cortexward.llm",
  "cortexward.agents",
  "cortexward.orchestrator",
  "cortexward.sandbox",
  "cortexward.storage",
  "cortexward.vcs",
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]

[[tool.importlinter.contracts]]
name = "Ports declare contracts only, no adapters"
type = "forbidden"
source_modules = ["cortexward.ports"]
forbidden_modules = [
  "cortexward.cpg",
  "cortexward.scanners",
  "cortexward.reporters",
  "cortexward.eval",
  "cortexward.llm",
  "cortexward.agents",
  "cortexward.orchestrator",
  "cortexward.sandbox",
  "cortexward.storage",
  "cortexward.vcs",
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]

[[tool.importlinter.contracts]]
name = "Plugin registry does not depend on concrete adapters"
type = "forbidden"
source_modules = ["cortexward.plugins"]
forbidden_modules = [
  "cortexward.cpg",
  "cortexward.scanners",
  "cortexward.reporters",
  "cortexward.eval",
  "cortexward.llm",
  "cortexward.agents",
  "cortexward.orchestrator",
  "cortexward.sandbox",
  "cortexward.storage",
  "cortexward.vcs",
]

[[tool.importlinter.contracts]]
name = "CPG engine does not depend on other adapters or interfaces"
type = "forbidden"
source_modules = ["cortexward.cpg"]
forbidden_modules = [
  "cortexward.scanners",
  "cortexward.reporters",
  "cortexward.eval",
  "cortexward.llm",
  "cortexward.agents",
  "cortexward.orchestrator",
  "cortexward.sandbox",
  "cortexward.storage",
  "cortexward.vcs",
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]

[[tool.importlinter.contracts]]
name = "Scanner adapters do not depend on other adapters or interfaces"
type = "forbidden"
source_modules = ["cortexward.scanners"]
forbidden_modules = [
  "cortexward.cpg",
  "cortexward.reporters",
  "cortexward.eval",
  "cortexward.llm",
  "cortexward.agents",
  "cortexward.orchestrator",
  "cortexward.sandbox",
  "cortexward.storage",
  "cortexward.vcs",
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]

[[tool.importlinter.contracts]]
name = "Reporters do not depend on other adapters or interfaces"
type = "forbidden"
source_modules = ["cortexward.reporters"]
forbidden_modules = [
  "cortexward.cpg",
  "cortexward.scanners",
  "cortexward.eval",
  "cortexward.llm",
  "cortexward.agents",
  "cortexward.orchestrator",
  "cortexward.sandbox",
  "cortexward.storage",
  "cortexward.vcs",
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]

# cortexward.eval only needs cortexward.domain (Finding, SourceLocation) for
# the RunManifest, dataset, and matcher/metrics/harness primitives. `ward
# bench run` (cortexward-cli) does its own scanning via
# cortexward.orchestrator and passes the resulting Findings into
# cortexward.eval.harness.run_bench() as plain data -- this contract turned
# out not to need loosening at all once that command actually landed; the
# scanning orchestration belongs to the delivery surface calling in, not to
# the harness being called.
[[tool.importlinter.contracts]]
name = "Evaluation harness does not depend on other adapters or interfaces"
type = "forbidden"
source_modules = ["cortexward.eval"]
forbidden_modules = [
  "cortexward.cpg",
  "cortexward.scanners",
  "cortexward.reporters",
  "cortexward.llm",
  "cortexward.agents",
  "cortexward.orchestrator",
  "cortexward.sandbox",
  "cortexward.storage",
  "cortexward.vcs",
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]

[[tool.importlinter.contracts]]
name = "LLM adapters do not depend on other adapters or interfaces"
type = "forbidden"
source_modules = ["cortexward.llm"]
forbidden_modules = [
  "cortexward.cpg",
  "cortexward.scanners",
  "cortexward.reporters",
  "cortexward.eval",
  "cortexward.agents",
  "cortexward.orchestrator",
  "cortexward.sandbox",
  "cortexward.storage",
  "cortexward.vcs",
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]

[[tool.importlinter.contracts]]
name = "VCS adapters do not depend on other adapters or interfaces"
type = "forbidden"
source_modules = ["cortexward.vcs"]
forbidden_modules = [
  "cortexward.cpg",
  "cortexward.scanners",
  "cortexward.reporters",
  "cortexward.eval",
  "cortexward.llm",
  "cortexward.agents",
  "cortexward.orchestrator",
  "cortexward.sandbox",
  "cortexward.storage",
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]

[[tool.importlinter.contracts]]
name = "Storage adapters do not depend on other adapters or interfaces"
type = "forbidden"
source_modules = ["cortexward.storage"]
forbidden_modules = [
  "cortexward.cpg",
  "cortexward.scanners",
  "cortexward.reporters",
  "cortexward.eval",
  "cortexward.llm",
  "cortexward.agents",
  "cortexward.orchestrator",
  "cortexward.sandbox",
  "cortexward.vcs",
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]

[[tool.importlinter.contracts]]
name = "Sandbox adapters do not depend on other adapters or interfaces"
type = "forbidden"
source_modules = ["cortexward.sandbox"]
forbidden_modules = [
  "cortexward.cpg",
  "cortexward.scanners",
  "cortexward.reporters",
  "cortexward.eval",
  "cortexward.llm",
  "cortexward.agents",
  "cortexward.orchestrator",
  "cortexward.storage",
  "cortexward.vcs",
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]

# Agents coordinate scanners/LLM adapters directly (cortexward-agents already
# depends on cortexward-llm and cortexward-scanners), so -- like the
# orchestrator -- they are deliberately NOT held to the peer-adapter "no
# other adapters" contract. They still must not depend on the
# delivery/interface layers that call *into* them.
[[tool.importlinter.contracts]]
name = "Agents do not depend on interface/delivery layers"
type = "forbidden"
source_modules = ["cortexward.agents"]
forbidden_modules = [
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]

# The orchestrator is deliberately NOT held to the same "no other adapters"
# contract as its peers above: its whole job is coordinating them (it
# already depends on cortexward.scanners; cortexward.cpg/llm/reporters are
# expected as agent-driven capabilities land). It still must not depend on
# the delivery/interface layers that call *into* it.
[[tool.importlinter.contracts]]
name = "Orchestrator does not depend on interface/delivery layers"
type = "forbidden"
source_modules = ["cortexward.orchestrator"]
forbidden_modules = [
  "cortexward.cli",
  "cortexward.server",
  "cortexward.sdk",
]
